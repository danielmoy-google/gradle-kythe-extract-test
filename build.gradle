// I have no idea how to use gradle.
description ="""
Dummy container for testing kythe extraction.

Project name: ${project.name}
"""

apply plugin: 'java'

dependencies {
  testCompile('junit:junit:4.12')
}

repositories {
  maven { url 'http://repo1.maven.org/maven2' }
}

task hello {
  group 'wtf'
  description "Doesn't do anything useful"
  doLast {
    println 'What is this'
  }
}

task afterHello(dependsOn: 'hello') {
  doLast {
    println 'afer hello'
  }
}

jar {
  manifest {
    attributes 'Main-Class': 'com.google.test.Foo'
  }
}
